# 首个角色骨架与命名登记表

目录及命名规则见 [`../asset-workflow.md`](../asset-workflow.md)。本表只保存角色特有事实；空项不是默认确认值。

| 项目 | 当前值 / 待确认内容 |
| --- | --- |
| 技术代号 | `Player`，用户 2026-09-20 确认；不随显示名改变 |
| 发布身份 | `Player`；新资产无需 `Role` 前缀，旧格式仅保留兼容 |
| 骨架来源 | 待确认：已有个人骨架 / 新建 / 合法取得的第三方骨架 |
| Max 版本 | Max 2020，用户确认；工具保持 Python 2.7 / PySide2 兼容 |
| 控制绑定 | Biped，用户确认 |
| 游戏导出骨架 | 待确认：直接导出 Biped 或独立蒙皮骨架；不是控制器清单 |
| 独立根运动节点及导出路径 | 名称为 `Root`（大小写固定），用户确认；完整导出路径待首条 FBX 核对 |
| 骨盆 / 左右脚 / 左右脚趾路径 | 待核对，不强制改成某套模板名 |
| 骨架版本 | 首次确认后登记；层级变化需要兼容性检查 |
| 首轮持物姿态 | `Unarmed`：空手，用户确认 |
| 后续动作集 | 按实际武器类型 / 动作体系扩展，不使用笼统 `Armed`；具体武器类型尚未确定 |
| 角色比例与身高 | 待确认，Unity 中按真实米制核对；不靠后期任意缩放修正 |
| 制作帧率 | 建议 60 fps，新动作确认后固定；已有素材先保留原采样率，不直接改帧率 |
| 单位、前向和导出轴转换 | 待首条 FBX 核对；Max Z-up 与 Unity Y-up 的差异交给一致的导出流程处理 |
| Unity 动画类型 | 当前管线为 Generic，最终由首条测试 FBX 验证 |

## 用户确认记录

2026-09-20：用户确认 Biped、Max 2020、独立根运动节点名 `Root`，玩家代号为 `Player`。首轮做空手，后续有空手 / 武器收纳与手持武器两套移动、待机。

随后用户修订命名：使用 `Player_Unarmed_Idle` / `Player_Unarmed_Run`。后续按武器动作集扩展，例如 `Player_Sword_Idle`（Sword 仅为示例）。`Unarmed` 表示手中无武器，不表示装备栏无武器，也不表示收纳动画本身；姿态相同的收纳状态可共用空手动作。收纳 / 取出是独立切换动作，暂不要求首轮制作。

目前信息足够完成工程配置。完整骨骼路径、比例和采样率通过实际个人测试资产检查补齐；无需凭记忆编造，也无需为了命名好看重建骨架。

## 首次资产核对

- 参考姿势、蒙皮与游戏导出骨架匹配。
- 根节点和骨盆职责分开，位移与转角只提取一次。
- 检查一条直跑的实际距离、速度、循环边界和脚部接触。
- 修改并重新发布同一个动作，确认动画与资产引用仍有效。
- 真实验收完成后更新本表及 ANIM-001；此前不得标注已兼容。
